project:
  service: # describe each service with it's associated vm

  config:
    - id: "5"
      provider: "docker"
      type: "generic"
      targets:
        - "1"
      opts:
        sub_driver: "swarm"
        manager: true
    - id: "6"
      provider: "docker"
      type: "generic"
      targets:
        - "1"
      opts:
        sub_driver: "swarm"
        manager: false



  topology: # Describe service dependecies
    provider:
      - name: "openstack"
        region:
          - name: "CORE"
            vm:
              {% for i in range(3) %}

              - id: "{{i}}"
                flavor: "m1.small"
                image: "Ubuntu-16-04"
                key: "tosca_key"
                networks:
                  - "ece1548-net"
              {% endfor %}
